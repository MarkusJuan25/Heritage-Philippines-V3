# Heritage Philippines V3 — Web Application Environment Variables
#
# No environment variables are required by this foundational scaffold.
# Feature-specific variables (database connection, authentication/session
# secrets, transactional email, object storage) will be added here, with
# their purpose and local/staging/production requirements documented,
# when each of those features is implemented — see
# .claude/rules/validation-deployment.md.
